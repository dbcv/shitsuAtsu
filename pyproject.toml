[project]
name = "shitsuatsu"
version = "0.1.0"
requires-python = ">=3.9"

dependencies = [
    "torch",
    "torchvision",
    "pillow>=11.3.0",
    "einops>=0.8.2",
    "setuptools<81",
    "pycocotools>=2.0.11",
    "psutil>=7.2.2",
    "sam3",
    "django>=4.2.30",
    "dj-svg>=0.3.1",
    "psycopg[binary]>=3.2.13",
    "django-environ>=0.13.0",
]

[tool.uv.sources]
sam3 = { path = "./sam3", editable = true }
